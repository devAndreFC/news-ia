# =============================================================================
# CONFIGURAÇÕES GLOBAIS DO PROJETO - Newsletter Inteligente
# =============================================================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Preencha as variáveis com seus valores reais
# 3. NUNCA commite o arquivo .env (ele está no .gitignore)
#

# -----------------------------------------------------------------------------
# INTELIGÊNCIA ARTIFICIAL
# -----------------------------------------------------------------------------
# Chave da API OpenAI para funcionalidades de IA (geração de notícias, análise)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY="sua-chave-openai-aqui"

# -----------------------------------------------------------------------------
# AMBIENTE DE DESENVOLVIMENTO
# -----------------------------------------------------------------------------
# Modo de debug (1=ativo, 0=inativo)
# IMPORTANTE: Use 0 em produção
DEBUG=1

# -----------------------------------------------------------------------------
# BANCO DE DADOS
# -----------------------------------------------------------------------------
# URL de conexão com PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://django:django@db:5432/newsletter

# -----------------------------------------------------------------------------
# SEGURANÇA E REDE
# -----------------------------------------------------------------------------
# Hosts permitidos para o Django (separados por vírgula)
# Em produção, adicione seu domínio
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]

# -----------------------------------------------------------------------------
# PORTAS DOS SERVIÇOS
# -----------------------------------------------------------------------------
# Porta do backend Django
BACKEND_PORT=8000
# Porta do frontend React
FRONTEND_PORT=3000
# Porta do banco PostgreSQL
DATABASE_PORT=5432
# Porta do RabbitMQ
RABBITMQ_PORT=5672

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS
# -----------------------------------------------------------------------------
# Chave secreta do Django (será gerada automaticamente se não definida)
# SECRET_KEY="sua-chave-secreta-aqui"

# Configurações de email (para notificações)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=seu-email@gmail.com
# EMAIL_HOST_PASSWORD=sua-senha-de-app
# EMAIL_USE_TLS=True